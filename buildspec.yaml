version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto11
    commands:
      - echo "Installing dependencies"
  pre_build:
    commands:
      - echo "Nothing to do in pre_build phase"
  build:
    commands:
      - cd lambda
      - mvn clean install
artifacts:
  files: 'target/*.jar'
